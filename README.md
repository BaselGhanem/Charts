# 📊 X Academy - منصة تعليم إكسل التفاعلية (Excel Master Class)

![Version](https://img.shields.io/badge/version-2.0-blue.svg)
![Technology](https://img.shields.io/badge/HTML5-TailwindCSS-green.svg)
![License](https://img.shields.io/badge/License-MIT-orange.svg)

تطبيق ويب تفاعلي بالكامل (Single Page Application) يهدف لتعليم مهارات **Microsoft Excel** المتقدمة (تحديداً تعدد الصفحات والرسوم البيانية) بأسلوب ممتع يعتمد على التلعيب (Gamification).

المشروع مبني في ملف واحد (HTML/JS/CSS) ولا يحتاج إلى خوادم خلفية (Backend)، مما يجعله سهل النشر والاستخدام.

---

## ✨ المميزات الرئيسية

### 1. 👤 تخصيص التجربة (Personalization)
- **اختيار الجنس:** واجهة ترحيبية تتيح للمستخدم اختيار شخصيته (شب 🙋‍♂️ / صبية 🙋‍♀️).
- **مخاطبة ذكية:** يتغير محتوى النصوص، الأفعال (مذكر/مؤنث)، والألوان (أزرق/زهري) بناءً على الاختيار.
- **تعدد اللغات:** دعم كامل للغتين العربية (RTL) والإنجليزية (LTR) بضغطة زر.

### 2. 🎮 التلعيب (Gamification)
- **نظام النقاط (XP):** اكتساب نقاط خبرة عند إتمام الدروس.
- **الأوسمة (Achievements):** جوائز تظهر عند تحقيق إنجازات معينة (مثل: الضربة الأولى، ساحر الإكسل، السرعة).
- **لوحة المتصدرين (Leaderboard):** عرض وهمي للمنافسين لزيادة الحماس.
- **مؤشر التقدم:** حلقة دائرية توضح نسبة إنجاز الدورة.

### 3. 🛠️ محاكاة تفاعلية (Simulators)
- **Excel Simulator:** جداول HTML تفاعلية تحاكي بيئة إكسل الحقيقية (الخلايا، التبويبات، المعادلات).
- **Interactive Charts:** استخدام مكتبة **Chart.js** لرسم مخططات بيانية تتغير بناءً على اختيار المستخدم.

### 4. 🎓 الشهادات (Certification)
- إصدار شهادة إتمام فورية عند اجتياز الاختبار النهائي.
- يتم توليد الشهادة كصورة (PNG) تحتوي على اسم المستخدم وتاريخ الإنجاز باستخدام مكتبة `html2canvas`.

---

## 🚀 طريقة التشغيل

بما أن المشروع عبارة عن صفحة واحدة، فعملية التشغيل بسيطة جداً:

1. **حمل الملف:** قم بتحميل الملف `index.html`.
2. **التشغيل:** افتح الملف في أي متصفح حديث (Chrome, Edge, Firefox).
3. **ابدأ التعلم:** سجل اسمك وابدأ الرحلة!

> **ملاحظة:** تأكد من وجود اتصال بالإنترنت في المرة الأولى لتحميل المكتبات الخارجية (CDN).

---

## 🛠️ التقنيات المستخدمة

تم بناء المشروع باستخدام تقنيات الويب القياسية ومكتبات خفيفة عبر CDN:

- **Core:** HTML5, Vanilla JavaScript (ES6+).
- **Styling:** [Tailwind CSS](https://tailwindcss.com/) (للتصميم والاستجابة).
- **Icons:** [Lucide Icons](https://lucide.dev/) (للأيقونات العصرية).
- **Charts:** [Chart.js](https://www.chartjs.org/) (للرسوم البيانية).
- **Confetti:** [Canvas Confetti](https://www.kirilv.com/canvas-confetti/) (للمؤثرات الاحتفالية).
- **Export:** [html2canvas](https://html2canvas.hertzen.com/) (لتصدير الشهادة كصورة).

---

## 📂 هيكلية الكود

الكود منظم داخل ملف واحد لسهولة النقل، ويتبع الهيكلية التالية:

1. **State Management:** كائن `state` يدير حالة التطبيق (اللغة، الجنس، التقدم، النقاط).
2. **Localization Objects:** كائنات `t` تحتوي على النصوص بجميع الصيغ (`ar_male`, `ar_female`, `en`).
3. **Config Objects:**
   - `lessonsConfig`: يربط الدروس بنوع المحاكاة (Excel/Chart) والإجابات الصحيحة.
   - `chartConfig`: إعدادات الرسوم البيانية.
4. **Core Functions:**
   - `init()`: تهيئة التطبيق.
   - `renderContent()`: الموجه الرئيسي (Router) للتنقل بين الصفحات.
   - `renderExcelGrid()` / `renderChart()`: دوال الرسم للمحاكاة.
   - `verify()`: التحقق من إجابات المستخدم ومنحه النقاط.

---

## 🎨 كيفية التعديل والإضافة

### إضافة درس جديد
اذهب إلى الكائن `lessonsBase` في قسم السكربت وأضف مفتاحاً جديداً:

```javascript
'new_lesson': { 
    t: 'عنوان الدرس', 
    d: 'وصف الدرس', 
    p: ['نقطة 1', 'نقطة 2'], 
    s: 'السيناريو المطلوب', 
    o: ['خيار 1', 'خيار 2', 'خيار 3'],
    realExample: 'مثال واقعي',
    extra: 'تلميح إضافي'
}
